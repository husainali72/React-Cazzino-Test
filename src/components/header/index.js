import React from 'react';

const Header = () => {
    return(
        <header className="header">
            <span className="logo">Cazzino</span>
            <span className="user-icon"></span>
        </header>
    )
}

export default Header;